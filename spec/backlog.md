# Product backlog

> The running list of desired features/changes, and the parking lot for items that belong to no
> feature yet. One line each. Lightweight — don't over-spec here; items earn detail as they move
> through the pipeline. (In `tracker`/`none` board mode this file is just the parking lot, with no
> sprints and no status ceremony: park an item, then promote it to a real feature/change in its time.)

Status values: idea · specified · planned · in-sprint · done · rejected

Type values: capability (new behavior) · change (to an existing feature) · debt (a conscious shortcut:
code that matches its spec but is badly made; cite the ENG-N rule it bends if `engineering.md` has
one). When several debt rows cluster around one surface, pay them down as a normal bounded feature
(e.g. `harden-error-handling`) whose acceptance criteria are exactly these rows, then mark them done.

`rejected` is the parking lot's memory: a human explicitly ruled the item out, and the What/why column
holds the durable reason (scope, philosophy, or a real constraint; "not now" is not a rejection, that
row stays `idea`). Before adding a new item, match it against rejected rows by concept, not wording
("night theme" matches a rejected dark-mode row); on a match, surface the prior ruling for the human
to confirm or consciously reopen instead of re-litigating it from scratch.

| # | Item | What / why | Type | Status | Folder |
|---|------|------------|------|--------|--------|
| 2 | trusted-workflow-credentials | Close the pull-request-to-pull-request gap that `changes/chg-001` left open: pin the pull-request role's trust policy to a reusable workflow on the default branch (`job_workflow_ref`), and have that trusted workflow hand out credentials already narrowed to `pr-<number>` via an inline session policy. Restores the constitution's full "touches only what it owns" guarantee. Declined during feat-001 only because it decides the calling-workflow shape for the deploy-action feature, which is not yet specified — take it up there. See `od-2`. FOLDED IN 2026-08-14: feat-002 takes this on rather than deferring it again, because it decides the calling workflow's shape anyway, and because pinning trust to a default-branch workflow is also what lets a pull-request-triggered build satisfy the constitution's default-branch rule as written. Now carried by that feature's AC-7 and AC-8. **DECLINED 2026-08-15 — do not re-propose this from scratch.** `chg-001` decided not to build it, and the constitution now reads *Preview environments are not isolated from each other, by decision* rather than describing a gap awaiting a fix. Three reasons it was declined rather than deferred again. The trigger-based privilege split never needed it: an OIDC subject names what TRIGGERED a run, not which workflow file ran, so a pull request that edits skyhook's own workflow is still offered only the narrower credentials — `job_workflow_ref` bought the preview-to-preview cut alone. Whether AWS honours that claim at all was never settled (AWS's documentation and GitHub's contradict each other), and the probe built to settle it was retired unrun once nothing shipped depended on the answer. And only a repository collaborator can open a pull request that deploys, so the boundary would defend against someone who already holds write access. What holds instead is AC-19: skyhook narrows its own credentials to the one environment it claimed before the repository's Terraform runs, which stops an honest run reaching a sibling and does nothing to stop a run that declines to ask. Reopening this means reopening the constitution clause first. | capability | rejected | `spec/features/deploy-action` |
| 5 | preview-boundary-residue | What feat-002 leaves behind, recorded so it is planned around rather than rediscovered. (a) A pull-request run can ENUMERATE environment names under `registry/<repo>/` and `state/<repo>/` — the `ListBucket` prefix condition was widened to those two prefixes so Terraform can enumerate workspaces (plan D3, `hs-1`). Listing only: every `GetObject`, `PutObject` and `DeleteObject` grant stays confined to the `pr-*` namespace at the role, and to one single environment once skyhook's session policy narrows it. A possible refund is open — if `TF_WORKSPACE` selects a workspace without Terraform enumerating first, the `state/<repo>/` half of the widening becomes unnecessary. (b) A pull request can reach a SIBLING preview environment's state by editing the scaffolded workflow so the run never asks for narrowing; state holds resource attributes in the clear, including any credential the infrastructure generated for itself. That is the decision row 2 records, not a gap — see the constitution, *Preview environments are not isolated from each other*. | capability | idea | — |
| 7 | unconditional-record-delete-window | The store's `delete` is unconditional, so teardown's final record removal is a check-then-act: it re-confirms the record is still `released`, then deletes. A deploy reactivating in that gap loses its record — it then fails loudly at its own final update and says to re-run, so the failure is visible, but the window exists (`src/core/teardown.ts`, comment at the removal step). Close it by giving the store a conditional delete (S3 DeleteObject honours If-Match) plus fake-store and registry support — a feat-001 contract change. Accepted at prototype depth alongside the spec's mid-destroy sharp edge. | debt | idea | — |
| 6 | sweep-second-direction | The sweep's other half, cut from feat-003 (teardown): discovering and destroying skyhook-provisioned infrastructure that has NO registry record. The constitution defines the sweep as correcting the difference between what exists and what is recorded, in both directions; feat-003 ships only the recorded-but-should-be-gone direction. Prevention is the prototype's answer for the other one — the deploy path records before it provisions, so an unrecorded environment requires a lost or corrupted registry. Needs its own thinking: how to recognize skyhook-provisioned resources without a record (tags?), and how to stay on the right side of "absence of evidence is not evidence for destruction". | capability | idea | — |
| 4 | bootstrap-state-has-no-home | The bootstrap writes Terraform state to a local file in `.skyhook/bootstrap/`, and nothing says what to do with it. Committed, it can leak sensitive values and conflicts on every apply; uncommitted, losing the working tree strands real cloud resources Terraform can no longer manage — which is exactly what happened to the deadweight test repo on 2026-08-14. The trust anchor survived because it is *detected*; the bucket and roles did not, because they live in state. Recommended fix: after the first apply, migrate the state into the bucket skyhook just created, using the S3 backend with `use_lockfile` that D4 already commits to. That makes it durable, encrypted, versioned, and never in git. `init` should also write a `.gitignore` covering `.terraform/` and `*.tfstate*` while keeping `.terraform.lock.hcl` — but only alongside the migration, since ignoring the state without giving it a home makes the loss silent by design rather than accidental. | capability | done | `spec/features/backing-store/changes/chg-005` |
| 3 | hand-rolled-sigv4 | `src/adapters/aws/sigv4.ts` implements AWS request signing by hand, because the plan takes no runtime dependencies (D1) and so there is no AWS SDK. It fails closed — a signing bug yields a rejected request, never a wider grant — and its real proof is task 6.2 against a live account, per the constitution's rule that adapters are verified against a real environment rather than mocked. Replace with the AWS SDK if skyhook ever takes a dependency budget. The same file also reads `ListObjectsV2` XML with regexes rather than a parser. | debt | idea | — |
| 1 | hand-rolled-yaml-subset | `src/core/yaml.ts` parses a strict subset of YAML by hand rather than taking a dependency, to keep the prototype at zero runtime deps. It refuses anything outside the subset loudly, so it is safe — but it is a parser we now maintain. Replace with a real YAML library once skyhook takes a dependency budget (the deploy action will need a bundler anyway). | debt | idea | — |
